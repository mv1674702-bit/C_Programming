#include<stdio.h>
int main()
{
    int n;
    printf("enter the number:");
    scanf("%d",&n);
    if (n>0)
    {
        printf("no. is positive");
    }
    else
    {
        printf("no. is negative");
    }
}